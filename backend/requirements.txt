# ==========================================
# 🚀 WINGS 3.2 PRODUCTION REQUIREMENTS (LOCKED)
# 锁定基准: 2026-07-16 服务器 .venv 实际运行版本
# ==========================================

# ── Core Web & ASGI ──
fastapi==0.138.0
uvicorn[standard]==0.49.0
sse-starlette==3.4.5
starlette==1.3.1

# ── Database & Async Driver ──
SQLAlchemy==2.0.51
aiomysql==0.3.2
PyMySQL==1.2.0
alembic==1.18.5

# ── Task Queue & Cache ──
celery==5.6.3
kombu==5.6.2
redis[hiredis]==8.0.0

# ── Security & Utilities ──
pydantic==2.13.4
pydantic_core==2.46.4
PyJWT==2.13.0
cryptography==49.0.0
structlog==26.1.0
python-dotenv==1.2.2
python-multipart==0.0.32
bcrypt==5.0.0

# ── HTTP Client (DeepSeek API) ──
httpx==0.28.1

# ── Document & PDF Engine ──
reportlab==5.0.0
openpyxl==3.1.5

# ── Dev & Test (pip install -r requirements-dev.txt) ──
# ruff==0.8.0
# pytest==9.1.1
# pytest-asyncio==0.24.0
# watchfiles==1.2.0
